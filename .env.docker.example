# Copiá a .env.docker (no subir a git)
# docker compose up -d --build

# JSON de Firebase Admin (misma clave que whatsapp-worker)
FIREBASE_CREDENTIALS_PATH=./services/whatsapp-worker/wodsi-47ffb-firebase-adminsdk-fbsvc-94ac17a419.json

FIREBASE_PROJECT_ID=wodsi-47ffb

# Mercado Pago (producción o prueba)
MP_ACCESS_TOKEN=TEST-...

# URL pública de TU app (App Hosting o dominio propio)
APP_PUBLIC_URL=https://wodsi-47ffb.web.app
CORS_ORIGIN=https://wodsi-47ffb.web.app

# Puerto expuesto en el host para webhooks MP
BILLING_PORT=8788

# WhatsApp worker
OUTBOX_POLL_MS=15000
SCHEDULER_POLL_MS=60000
